apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: {{ .Values.namespace }}
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: prometheus
        static_configs:
          - targets: ['localhost:9090']

      - job_name: otelcol
        static_configs:
          - targets: ['otelcol:{{ .Values.otelcol.ports.metrics }}']

      - job_name: otelcol-exporter
        static_configs:
          - targets: ['otelcol:{{ .Values.otelcol.ports.prometheusExporter }}']
